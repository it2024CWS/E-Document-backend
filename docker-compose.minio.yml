services:
  minio:
    image: minio/minio:latest
    container_name: edocument-minio-dev
    restart: unless-stopped
    command: server /data --console-address ":9006"
    environment:
      - MINIO_ROOT_USER=admin
      - MINIO_ROOT_PASSWORD=password123
    ports:
      - "9005:9000"   # MinIO API
      - "9006:9001"   # MinIO Console
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9005/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - minio-network

networks:
  minio-network:
    driver: bridge

volumes:
  minio_data:
    driver: local

